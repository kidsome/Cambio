class Kings extends Special {
    constructor(number, suit, active){
    if(number != "king") {
        throw new error("OK MUST BE A KING!");
            }
            super(number, suit, active);
    
        }
        
    }